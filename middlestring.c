#include <stdio.h>
#include<string.h>
#include<math.h>
int main(void) {
	char a[67];
	float k;
	int l,h;
	printf("enter the element");
	scanf("%s",a);
	l=strlen(a);
	k=l/2;
	if((k%2)==0)
	{
		middle=
	}

	return 0;
}
